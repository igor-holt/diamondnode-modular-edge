# Podman × Turing × JAX × HyperNEAT-LEO → Modular Differentiation

**Core objective mapping**
1. Intrinsic Pursuit — evolve modular substrates (LEO-driven intra-module density, inter-module sparsity). Geometry produces differentiation.
2. Financial Infrastructure — each GTX 1650 becomes an independent modular topology factory; results feed QOS / combinatorial services.
3. Hybridization — OpenClaw agents + Diamondnode energy landscape consume the same fingerprint-locked modular J.

## Why this stack

- **Podman** (rootless) per Turing GPU → isolation, lower attack surface than Docker daemon, works cleanly with NVIDIA Container Toolkit.
- **JAX + Equinox** inside the container → same PyTree CPPN-LEO we already validated; CUDA path for the 1650.
- **HyperNEAT-LEO / modular fitness** → the classic mechanism that produces modular differentiation (separate functional modules via geometric Link Expression Output).

## Operator path (second GTX 1650)

```bash
cd podman-turing-hyperneat
bash scripts/podman_build_and_run.sh
# or
podman build -t diamondnode-turing-hyperneat -f container/Containerfile .
podman run --device nvidia.com/gpu=all -v /data/diamondnode-dual:/data:Z \
    diamondnode-turing-hyperneat --n-nodes 16 --pop 20 --gens 30
```

Result lands as a fingerprint-locked modular_result.json (J matrix + stats). Master control-plane or the OpenClaw LEO skill can ingest it directly into the EnergyLandscape or geometric routing.

## Invariants

- Hermitian symmetry option remains available for self-adjoint energy operators.
- Every substrate carries a content fingerprint.
- Maru #!nox still applies on R>0.4 or attempts to force dense non-modular topologies onto constrained hardware.
- Thermodynamic accounting (sparsity → active parameters) is first-class.
